<nav class="navmenu" id="navbar">
        <ul class="container">
            <li class="grow"><a href="#portfolio">About Us</a></li>
            <li class="grow"><a href="#plans">Plans</a></li>
            <li class="grow"><a href="/">SHIBUMI</a></li>
            <li class="grow"><a href="#portfolio">Portfolio</a></li>
            <li class="grow"><a href="#contact">Contact Us</a></li>
        </ul>  
</nav>

<style>

    a, li {
        text-decoration: none;
        color: white;
        font-size: 25px;;
        font-family: 'Lora', serif;
        line-height: 35px;
    }

    a:hover {
     color: #FFEA00;
    }

    .grow {
        transition: all .3s ease-in-out;
    }

    .grow:hover {
        text-shadow: 0 0 7px;
        text-shadow: 4px 4px 8.3px 0px #FDB82780;
        transform: scale(1.1);
    }

    li {
        list-style-type: none;
    }

    .navmenu {
        display: flex;
        flex-direction: row;
        align-items: center;
        width: 100%;
        position: -webkit-sticky;
        position: sticky;
        top: 0;
        z-index: 1000;
        background-color: transparent;
        padding: 5px 0; 
        transition: background-color 0.3s ease;
    }

    .navmenu.scrolled {
        background-color: rgb(23,27,33, 0.9);
    }


    .container {
        display: flex;
        flex-direction: row;
        justify-content: space-around;
        width: 100%;
    }

</style>

<script>
    document.addEventListener('DOMContentLoaded', function () {
    const navbar = document.getElementById('navbar');
    window.addEventListener('scroll', function () {
      if (window.scrollY > 0) {
    //@ts-ignore
        navbar.classList.add('scrolled');
      } else {
    //@ts-ignore
        navbar.classList.remove('scrolled');
      }
    });
  });
</script>