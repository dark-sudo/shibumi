<div class="plans">
    <h1>I WANT</h1>
    <div class="cards">
        <div class="card">
            <h2>E-Commerce Website</h2>
            <img src="/public/Plans/cart 1.svg" alt="">
        </div>
        <div class="card">
            <h2>Showcase Website</h2>
            <img src="/public/Plans/artist 1.svg" alt="">
        </div>
        <div class="card">
            <h2>Custom Website</h2>
            <img src="/public/Plans/custum 1.svg" alt="">
        </div>
    </div>
</div>

<style>
    .plans {
        display: flex;
        flex-direction: column;
        align-items: center;
        justify-content: space-between;
        width: 90%;
        border: 1px solid red;
        align-self: flex-start;
        height: 90%;
        gap: -80px;

    }

    h1 {
        font-size: 239px;
        font-family: 'Fjalla One';
        background: -webkit-linear-gradient(279.88deg, #FFFFFF -45.21%, #0019FA 119.75%);
        -webkit-background-clip: text;
        -webkit-text-fill-color: transparent;
        border: 1px solid blue;
        margin:0px;
    }

    h2 {
        font-family: 'Fjalla One';
        font-size: 33px;
        line-height: 41%;
        padding-top: 30px;
    }

    .cards {
        display: flex;
        flex-direction: row;
        justify-content: space-around;
        width: 85%;
        height: 50%;
        border: 1px solid green;
    }

    .card {
        display: flex;
        flex-direction: column;
        align-items: center;
        justify-content: space-between;
        width:25%;
        height: 100%;
        border-radius: 34px;
        backdrop-filter: blur(50px);
        background-image: -moz-linear-gradient(135deg, rgb(179, 231, 255), rgb(255, 253, 194));
        background-image: -webkit-linear-gradient(135deg, rgb(179, 231, 255), rgb(255, 253, 194));
        background-image: linear-gradient(135deg, rgb(179, 231, 255), rgb(255, 253, 194));
    }

    img {
        height: 310px;
    }


.abox:before {
    content: "";
    z-index: -1;
    position: absolute;
    top: 0;
    right: 0;
    bottom: 0;
    left: 0;
    background: linear-gradient(10deg, #60efff 40%, #00ff87 100% );
    transform: translate3d(0px, 10px, 0) scale(0.95);
    filter: blur(20px);
    opacity: var(0.9);
    transition: opacity 0.6s;
    border-radius: inherit;
}

/* 
* Prevents issues when the parent creates a 
* stacking context. (For example, using the transform
* property )
*/
.abox::after {
    content: "";
    z-index: -1;
    position: absolute;
    top: 0;
    right: 0;
    bottom: 0;
    left: 0;
    background: inherit;
    border-radius: inherit;
}
        
</style>